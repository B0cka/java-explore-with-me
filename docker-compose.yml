services:
  stats-server:
    image: stats-server
    build:
      context: ./stats-service
      dockerfile: Dockerfile
    ports:
      - "9090:9090"

  stats-db:
    image: postgres:16.1

  ewm-service:
    image: main-service
    build:
      context: ./main-service
      dockerfile: Dockerfile
    ports:
      - "8080:8080"
  main-db:
    image: postgres:16.1
